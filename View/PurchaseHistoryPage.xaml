<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="E_Raamatud.PurchaseHistoryPage"
             Title="Ostude Ajalugu">

    <ContentPage.Background>
        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#406780" Offset="0.0" />
            <GradientStop Color="#549082" Offset="1.0" />
        </LinearGradientBrush>
    </ContentPage.Background>

    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="15">
            <Label Text="Sinu ostude ajalugu"
                   FontSize="28"
                   FontAttributes="Bold"
                   TextColor="White" />

            <CollectionView x:Name="PurchaseList" HeightRequest="400" BackgroundColor="#300000FF">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Frame BorderColor="White" BackgroundColor="#FFFFFF30" CornerRadius="10" Padding="10">
                            <VerticalStackLayout>
                                <Label Text="{Binding BookTitle}" FontSize="18" FontAttributes="Bold" TextColor="White"/>
                                <Label Text="{Binding Quantity, StringFormat='Kogus: {0}'}" TextColor="White"/>
                                <Label Text="{Binding TotalPrice, StringFormat='Hind kokku: €{0:F2}'}" TextColor="White"/>
                            </VerticalStackLayout>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
